# here is the command for importing the data from MYsql to HDFS option With tareget directory and mapper
command 
sqoop import --connect jdbc:mysql://quickstart.cloudera/retail_db --username root --password cloudera \
--table products \
--fields-terminated-by '\t' \
--m1 \
--target-dir-itemsdata

sqoop command to connect
sqoop import --connect jdbc:mysql://gateway/claim/ --table customer --username cloudera --password cloudera

we can import the table  from database;
command
sqoop import --connect jdbc:mysql://quickstaret.cloudera/retail_db \
--username root --password cloudera \
--table products \
--fields-terminated-by '\t' \
--m1 \
--target-dir-itemdata


sqopop import --connect jdbc:mysql://quickstart.clouedra/reatil_db --username cloudera --password clouedra --table cliam  \
--fields-terminated-by '\t'
-m2 ( Mapper indication how many ,mapper do we neeed)
--target-dir-claimdata

Command: sqoop list-databases 
Explanation: using below command you able to see the data base listing in you s
sqoop list-databases --connect jdbc:mysql://quickstart.cloudera --username root --password cloudera

#Command: sqoop list-tables \
#Explanation: To list the all tables in the retail_db(ANY) database.
sqoop list-tables --connect jdbc:mysql://quickstart.cloudera/retail_db --username root --password cloudera


